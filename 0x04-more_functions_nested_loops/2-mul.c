#include "main.h"
/**
*mul - multiplies two integers
*@a: The first number to be multiply
*@b: The second number to be multiply
*Return: The result of the multiplication between a and b parameters
*/
int mul(int a, int b)
{
return (a * b);
}
